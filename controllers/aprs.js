const createPkt = (data) => {
    return new Promise((resolve, reject) => {
        const currTime = new Date()
        console.log('eventual APRS function')
        resolve()
    })
}

module.exports = {
    createPkt
}